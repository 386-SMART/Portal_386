<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panel Administración Comercial</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@400;700&display=swap" rel="stylesheet">
    <link rel="shortcut icon" href="/img/favicon-16x16.ico" type="image/x-icon">
    
    <style>
        :root {
            --brand-blue: #0080FF;
            --sidebar-open-width: 260px;
            --sidebar-closed-width: 88px;
            --transition-speed: 0.3s;
        }
        body {
            font-family: 'Lato', sans-serif;
            background-color: #f4f7f6;
            overflow-y: hidden;
        }
        
        /* Contenedor principal para el layout de 2 columnas */
        .main-wrapper {
            display: flex;
            min-height: 100vh;
        }
        
        /* --- Sidebar Estructura Base --- */
        .sidebar {
            width: var(--sidebar-open-width);
            background-color: var(--brand-blue);
            color: white;
            padding: 1rem;
            display: flex;
            flex-direction: column;
            transition: width var(--transition-speed) ease;
        }
        .sidebar.collapsed {
            width: var(--sidebar-closed-width);
        }
        
        /* --- Encabezado --- */
        .sidebar-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            min-height: 50px;
        }
        .logo-details {
            display: flex;
            align-items: center;
            opacity: 1;
            transition: opacity 0.2s ease;
        }
        .logo-details .bi-headset { font-size: 2rem; }
        .logo-details .logo-name { font-size: 1.5rem; font-weight: 700; margin-left: 10px; white-space: nowrap; }
        
        #btn-toggle {
            font-size: 1.75rem;
            cursor: pointer;
            color: white;
            background: none;
            border: none;
        }

        /* --- Lista de Navegación --- */
        .nav-list {
            flex-grow: 1;
            margin-top: 1rem;
        }
        .nav-link {
            font-size: 1.1rem;
            color: rgba(255, 255, 255, 0.8);
            border-radius: .375rem; /* Ajuste para un contorno más suave */
            margin-bottom: 0.5rem;
            padding: 0.75rem; /* Padding consistente */
            display: flex;
            align-items: center;
            justify-content: flex-start;
        }
        .nav-link:hover { background-color: rgba(255, 255, 255, 0.1); color: white; }
        .nav-link.active { background-color: white; color: var(--brand-blue); font-weight: 700; }
        
        .nav-link .bi {
            width: 40px; /* Ancho fijo para el área del icono */
            font-size: 1.5rem;
            flex-shrink: 0;
            text-align: center;
        }
        .link-name {
            white-space: nowrap;
            opacity: 1;
            transition: opacity 0.2s ease;
            margin-left: 10px; /* Espaciado entre icono y texto */
        }

        /* --- Perfil y Logout --- */
        .profile-details {
            opacity: 1;
            transition: opacity 0.2s, height 0.2s;
            height: auto;
        }
        .profile-details .name { font-weight: 700; }
        .profile-details .small { opacity: 0.8; }
        
        #logout-btn {
            background-color: rgba(255, 255, 255, 0.15);
            padding: 0.75rem;
        }
        #logout-btn:hover { background-color: rgba(255, 255, 255, 0.25); }

        /* --- ESTILOS PARA MODO COLAPSADO --- */
        .sidebar.collapsed .logo-details,
        .sidebar.collapsed .link-name,
        .sidebar.collapsed .profile-details {
            opacity: 0;
            pointer-events: none;
            display: none;
        }
        .sidebar.collapsed .sidebar-header {
            justify-content: center;
        }
        .sidebar.collapsed .nav-link,
        .sidebar.collapsed #logout-btn {
            justify-content: center;
        }

        /* --- Contenido Principal --- */
        .content-area {
            flex-grow: 1;
            height: 100vh;
            padding: 0;
        }
        #content-frame { width: 100%; height: 100%; border: none; }
    </style>
</head>
<body>

<div class="main-wrapper">
    <div id="sidebar" class="sidebar d-flex flex-column">
        <div class="sidebar-header">
            <a href="#" class="logo-details text-white text-decoration-none">
                <i class="bi bi-headset"></i>
                <span class="logo-name">Cass TI</span>
            </a>
            <button class="btn" id="btn-toggle"><i class="bi bi-list"></i></button>
        </div>
        
        <ul class="nav nav-pills flex-column mb-auto nav-list">
             <% menuItems.forEach((item, index) => { %>
                <li class="nav-item">
                    <a href="<%= item.path %>" class="nav-link menu-link d-flex align-items-center <%= index === 0 ? 'active' : '' %>" target="content-frame">
                        <i class="bi <%= item.icon %>"></i>
                        <span class="link-name"><%= item.label %></span>
                    </a>
                </li>
            <% }); %>
        </ul>
        
        <hr class="text-white-50">
        <div class="profile-details mb-2">
            <div class="text-white">
                <div class="fw-bold"><%= user.nombre %></div>
                <div class="small"><%= user.empresaName %></div>
            </div>
        </div>
        <a href="#" class="btn d-flex align-items-center text-white" id="logout-btn">
             <i class="bi bi-box-arrow-left"></i>
             <span class="link-name">Cerrar Sesión</span>
        </a>
    </div>

    <main class="content-area">
        <iframe id="content-frame" name="content-frame" src="<%= menuItems[0].path %>"></iframe>
    </main>
</div>

<script src="/js/comercial.js"></script>
</body>
</html>