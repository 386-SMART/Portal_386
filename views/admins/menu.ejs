<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css" rel="stylesheet">

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <title>
    <%= isAdmin ? 'Panel Admin' : 'Panel Cliente' %>
  </title>

  <link rel="stylesheet" href="/css/menu.css">
  <link href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="shortcut icon" href="/img/favicon-16x16.ico" type="image/x-icon">

  <script>
    window.__IS_ADMIN__ = "<%= isAdmin %>";
    window.__TENANT_SLUG__ = "<%= user.slug %>";
    window.__USER_NAME__ = "<%= user.nombre %>";
  </script>
</head>

<body>
  <div id="page-loader-overlay" class="page-loader-overlay">
    <div class="spinner"></div>
  </div>

  <div class="d-flex">
    <nav class="sidebar">

      <div class="logo-details">
        <div class="logo-wrapper">
          <img src="<%= user.logo ? '/img/logo_cliente/' + user.logo : '/img/logo386blanco.png' %>" alt="Logo"
            id="sidebar-logo" />
        </div>
        <i class='bx bx-menu' id="btn-toggle"></i>
      </div>

      <ul class="nav-list">
        <% menuItems.forEach(item=> { %>
          <li>
            <a href="#" class="sidebar-link<%= item.active ? ' active' : '' %>" data-path="<%= item.path %>">
              <i class="<%= item.icon %>"></i>
              <span class="links_name">
                <%= item.label %>
              </span>
            </a>
            <span class="tooltip">
              <%= item.label %>
            </span>
          </li>
          <% }) %>

            <li class="spacer"></li>

            <% if (!isAdmin && userCompanies && userCompanies.length> 1) { %>
              <li class="company-switcher-container" id="company-switcher-menu">
                <a href="#" class="switcher-toggle">
                  <i class='bx bxs-buildings'></i>
                  <span class="links_name">Cambiar Empresa</span>
                </a>
                <span class="tooltip">Cambiar Empresa</span>

                <ul class="sub-menu">
                  <% userCompanies.forEach(company=> {
                    const isActive = company.id_empresa == activeTenant.id_empresa;
                    %>
                    <li>
                      <a href="#" class="company-option <%= isActive ? 'active-company' : '' %>"
                        data-empresa-id="<%= company.id_empresa %>">
                        <i class='bx <%= isActive ? ' bx-check-circle' : 'bx-building' %>'></i>
                        <span class="company-name">
                          <%= company.nombre %>
                        </span>
                      </a>
                    </li>
                    <% }) %>
                </ul>
              </li>
              <% } %>
      </ul>

      <div class="profile-container">
        <div class="profile">
          <div class="profile-content">
            <div class="avatar">
              <%= user.nombre.charAt(0).toUpperCase() %>
            </div>
            <div class="profile-details">
              <div class="name">
                <%= user.nombre.split(' ')[0] %></div>
              <div class="role"><%= isAdmin ? ' Administrador' : 'Cliente' %>
              </div>
            </div>
          </div>
          <i class='bx bx-log-out' id="log_out" title="Cerrar Sesión"></i>
        </div>
      </div>
    </nav>

    <section class="home-section">
      <div id="welcome-container" class="welcome-screen"></div>

      <div id="iframe-loader" class="loader-container d-none">
        <div class="spinner"></div>
      </div>

      <iframe id="content-frame" frameborder="0" style="display:none;"></iframe>
    </section>
  </div>

  <script src="/js/menu.js"></script>
</body>

</html>