<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestión de Técnicos</title>
    <meta name="csrf-token" content="<%= csrfToken %>">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
</head>
<body class="bg-light">

<div class="container py-5">
    <h2 class="fw-bold mb-4">Gestión de Técnicos de Soporte TI</h2>
    <div class="row g-4">
        <!-- COLUMNA IZQUIERDA: TÉCNICOS -->
        <div class="col-lg-4">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h5 class="card-title fw-bold mb-3">Registrar Nuevo Técnico</h5>
                    <form id="form-tecnico">
                        <input type="hidden" id="tecnico-id">
                        <div class="mb-3">
                            <label for="nombre" class="form-label">Nombre</label>
                            <input type="text" id="nombre" class="form-control" required>
                        </div>
                        <div class="mb-3">
                            <label for="apellido" class="form-label">Apellido</label>
                            <input type="text" id="apellido" class="form-control" required>
                        </div>
                        <div class="mb-3">
                            <label for="dni" class="form-label">DNI</label>
                            <input type="text" id="dni" class="form-control" required>
                        </div>

                        <div class="mb-4">
                            <label for="firma" class="form-label fw-bold">Firma Digital</label>
                            <input type="file" id="firma" class="form-control" accept="image/png, image/jpeg, image/jpg">
                            <div class="form-text text-muted">Subir imagen (PNG/JPG) con fondo transparente preferiblemente. Máx 2MB.</div>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" id="btn-save" class="btn btn-primary">Registrar</button>
                            <button type="button" id="btn-cancel" class="btn btn-secondary d-none">Cancelar Edición</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        
        <!-- COLUMNA CENTRO: TÉCNICOS REGISTRADOS -->
        <div class="col-lg-4">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h5 class="card-title fw-bold mb-3">Técnicos Registrados</h5>
                    <div class="table-responsive">
                        <table class="table table-hover align-middle">
                            <thead class="table-light">
                                <tr>
                                    <th>Nombre Completo</th>
                                    <th>DNI</th>
                                    <th class="text-end">Acciones</th>
                                </tr>
                            </thead>
                            <tbody id="tecnicos-table-body">
                                </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <!-- COLUMNA DERECHA: USUARIOS CLIENTE -->
        <div class="col-lg-4">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h5 class="card-title fw-bold mb-3">
                        <i class="bi bi-person-plus"></i> Crear Usuario Login
                    </h5>
                    <p class="text-muted small">Usuarios para que técnicos se logueen en el sistema</p>
                    <form id="form-usuario-cliente">
                        <input type="hidden" id="usuario-id">
                        <div class="mb-3">
                            <label for="usuario-nombre" class="form-label">Nombre Completo</label>
                            <input type="text" id="usuario-nombre" name="usuario-nombre" class="form-control" required>
                        </div>
                        <div class="mb-3">
                            <label for="usuario-username" class="form-label">Usuario (Username)</label>
                            <input type="text" id="usuario-username" name="usuario-username" class="form-control" required>
                        </div>
                        <div class="mb-3" id="pass-container">
                            <label for="usuario-password" class="form-label">Contraseña</label>
                            <input type="password" id="usuario-password" name="usuario-password" class="form-control">
                        </div>
                        <div class="mb-3" id="passconf-container">
                            <label for="usuario-confirm-password" class="form-label">Confirmar Contraseña</label>
                            <input type="password" id="usuario-confirm-password" name="usuario-confirm-password" class="form-control">
                        </div>
                        <div class="mb-3" id="btn-show-pass-container" style="display: none;">
                            <button type="button" id="btn-show-pass" class="btn btn-sm btn-outline-warning">
                                <i class="bi bi-key-fill"></i> Cambiar Contraseña
                            </button>
                        </div>
                        <small class="text-muted d-block mb-3">Empresa: 386 SMART S.A.C (Fija)</small>
                        <div class="d-grid gap-2">
                            <button type="submit" id="btn-save-usuario" class="btn btn-success">
                                <i class="bi bi-person-plus"></i> Crear Usuario
                            </button>
                            <button type="button" id="btn-cancel-usuario" class="btn btn-secondary d-none">
                                Cancelar Edición
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- TABLA DE USUARIOS CLIENTE -->
    <div class="row mt-4">
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h5 class="card-title fw-bold mb-3">
                        <i class="bi bi-list"></i> Usuarios Cliente Registrados (Empresa 386 SMART)
                    </h5>
                    <div class="table-responsive">
                        <table class="table table-hover align-middle">
                            <thead class="table-light">
                                <tr>
                                    <th>Nombre</th>
                                    <th>Usuario (Username)</th>
                                    <th>Fecha Creación</th>
                                    <th class="text-end" style="width: 150px;">Acciones</th>
                                </tr>
                            </thead>
                            <tbody id="usuarios-table-body">
                                </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="/js/gestion-tecnicos.js"></script>
</body>
</html>
