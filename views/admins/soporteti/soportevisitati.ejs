<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestión de Informes de Campo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.11.4/css/lightbox.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/apexcharts"></script>

    <style>
        .report-preview { font-family: Arial, sans-serif; font-size: 12px; color: #000; }
        .report-header { text-align: center; font-weight: bold; font-size: 16px; margin-bottom: 20px; }
        .report-section { border: 1px solid #000; margin-bottom: 10px; }
        .report-section-title { background-color: #e9ecef; font-weight: bold; padding: 4px 8px; border-bottom: 1px solid #000; }
        .report-body { padding: 8px; }
        .report-field { display: flex; margin-bottom: 4px; }
        .report-field label { font-weight: bold; width: 120px; flex-shrink: 0; }
        .report-field span { border-bottom: 1px dotted #000; width: 100%; display: block; padding-left: 5px; }
        .pagination .page-link { cursor: pointer; }
    </style>
</head>
<body class="bg-light">

<div class="container my-5">
    <h2 class="mb-4"><i class="bi bi-folder-check"></i> Gestión de Informes de Campo</h2>

    <div class="row mb-4">
        <div class="col-lg-6 mb-4 mb-lg-0">
            <div class="card h-100">
                <div class="card-header fw-bold">Enlace para Nuevo Informe</div>
                <div class="card-body">
                    <p class="card-text">Copia y envía este enlace a un técnico.</p>
                    <div class="input-group">
                        <input type="text" class="form-control" value="<%= tecnicoLink %>" id="link-tecnico" readonly>
                        <button class="btn btn-secondary" onclick="copiarLink()"><i class="bi bi-clipboard"></i> Copiar</button>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-lg-6">
            <div class="card h-100">
                <div class="card-header fw-bold">Técnicos más Activos</div>
                <div class="card-body">
                    <div id="tecnicosChart"></div>
                </div>
            </div>
        </div>
    </div>

    <h3 class="mt-5">Historial de Informes</h3>
    
    <div class="card mb-3">
        <div class="card-body">
            <div class="row g-3">
                <div class="col-md-3">
                    <label class="form-label">Empresa</label>
                    <select id="filtroEmpresa" class="form-select">
                        <option value="">Todas las empresas</option>
                    </select>
                </div>
                <div class="col-md-3">
                    <label class="form-label">N° Serie</label>
                    <input type="text" id="filtroSerie" class="form-control" placeholder="Buscar por serie...">
                </div>
                <div class="col-md-3">
                    <label class="form-label">Ticket</label>
                    <input type="text" id="filtroTicket" class="form-control" placeholder="Buscar por ticket...">
                </div>
                <div class="col-md-3 d-flex align-items-end">
                    <button id="btnBuscar" class="btn btn-primary me-2"><i class="bi bi-search"></i> Buscar</button>
                    <button id="btnLimpiar" class="btn btn-secondary"><i class="bi bi-x-circle"></i> Limpiar</button>
                </div>
            </div>
        </div>
    </div>
    
    <div class="card shadow-sm">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>Fecha Servicio</th>
                            <th>Empresa</th>
                            <th>Ticket</th>
                            <th>N/S Equipo</th>
                            <th>Técnico</th>
                            <th>Estado Final</th>
                            <th class="text-end">Acciones</th>
                        </tr>
                    </thead>
                    <tbody id="tabla-informes"></tbody>
                </table>
            </div>
            <nav id="pagination-container" aria-label="Navegación de informes">
                <ul class="pagination justify-content-center"></ul>
            </nav>
        </div>
    </div>
</div>

<div class="modal fade" id="informeModal" tabindex="-1"><div class="modal-dialog modal-lg modal-dialog-scrollable"><div class="modal-content"><div class="modal-header"><h5 class="modal-title">Detalles del Informe de Campo</h5><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body" id="modal-body-content"></div><div class="modal-footer"><button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button></div></div></div></div>

<script>
    function copiarLink() {
        const linkInput = document.getElementById('link-tecnico');
        linkInput.select();
        document.execCommand('copy');
        alert('Enlace copiado');
    }
</script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.0/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.11.4/js/lightbox.min.js"></script>
<script src="/js/gestionvisitas.js"></script>

</body>
</html>