<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Iniciar Sesión - 386 Smart</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css"/>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="shortcut icon" href="/img/favicon-16x16.ico" type="image/x-icon">
    
    <style>
        :root {
            --if-primary: #0d6efd;
            --if-text-main: #212529;
            --if-text-muted: #6c757d;
            --if-bg-main: #f8f9fc;
            --if-bg-panel: #ffffff;
            --if-border-color: #e2e8f0;
            --if-font-sans: 'Inter', sans-serif;
        }
        html, body { height: 100%; margin: 0; font-family: var(--if-font-sans); overflow: hidden; }
        .login-container { display: flex; width: 100%; min-height: 100vh; }
        .left-pane { flex: 0 0 45%; background-color: var(--if-primary); color: #fff; display: flex; flex-direction: column; justify-content: center; align-items: center; padding: 3rem; text-align: center; position: relative; }
        #particles-js { position: absolute; width: 100%; height: 100%; top: 0; left: 0; z-index: 1; }
        .welcome-content { position: relative; z-index: 2; }
        .welcome-content .logo { max-width: 200px; margin-bottom: 2rem; }
        .welcome-content .greeting-title { font-weight: 700; font-size: 3rem; }
        .right-pane { flex: 1; background-color: var(--if-bg-panel); display: flex; justify-content: center; align-items: center; padding: 2rem; }
        .login-form-wrapper { width: 100%; max-width: 420px; }
        .login-form-wrapper h2 { font-weight: 700; color: var(--if-text-main); }
        .form-control:focus { border-color: var(--if-primary); box-shadow: 0 0 0 3px rgba(13, 110, 253, 0.1); }
        .btn-primary { padding: 0.8rem; font-weight: 600; }
        .input-group .form-control { border-right: 0; }
        .input-group .btn { border-left: 0; background-color: white; }
        .input-group .btn:hover, .input-group .btn:focus { background-color: #f8f9fa; }
        @media (max-width: 991px) {
            .left-pane { display: none; }
            .right-pane { align-items: flex-start; padding-top: 5rem; }
        }
    </style>
</head>
<body>

    <div class="login-container">
        <div class="left-pane">
            <div id="particles-js"></div>
            <div class="welcome-content">
                <img src="/img/nuevo_logo_386_blanco.png" alt="Logo" class="logo">
                <h1 id="greeting" class="greeting-title">Bienvenido</h1>
                <p class="slogan fs-5">Portal Web.</p>
            </div>
        </div>

        <div class="right-pane">
            <div class="login-form-wrapper">
                <h2 class="mb-2">Iniciar Sesión</h2>
                <p class="text-muted mb-4">Ingresa tus credenciales para acceder al panel.</p>

                <div id="alert-error" class="alert alert-danger d-none" role="alert"></div>
                
                <form id="login-form" autocomplete="off" novalidate>
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <div class="form-floating mb-3">
                        <input type="text" class="form-control" id="username" placeholder="Usuario" required maxlength="50">
                        <label for="username">Usuario</label>
                    </div>
                    
                    <div class="input-group mb-4">
                        <div class="form-floating">
                        <input type="password" class="form-control" id="password" placeholder="Contraseña" required maxlength="100">
                        <label for="password">Contraseña</label>
                        </div>
                        <button type="button" id="togglePassword" class="btn btn-outline-secondary"><i class="bi bi-eye-slash"></i></button>
                    </div>

                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary">Ingresar</button>
                    </div>

                    <div class="text-center mt-3" id="loader" style="display: none;">
                        <div class="spinner-border text-primary" role="status"><span class="visually-hidden">Cargando...</span></div>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/particles.js@2.0.0/particles.min.js"></script>
    <script src="/js/login.js"></script>

</body>
</html>