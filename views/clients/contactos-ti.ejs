<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contactos TI</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="shortcut icon" href="/img/favicon-16x16.ico" type="image/x-icon">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <style>
        :root {
            --if-primary: #0d6efd;
            --if-success: #198754;
            --if-text-main: #212529;
            --if-text-muted: #6c757d;
            --if-bg-main: #f8f9fc;
            --if-bg-panel: #ffffff;
            --if-border-color: #e2e8f0;
            --if-font-sans: 'Inter', sans-serif;
        }

        body {
            font-family: var(--if-font-sans);
            background-color: var(--if-bg-main);
        }

        .page-header h1 { font-weight: 700; }

        .content-card {
            background-color: var(--if-bg-panel);
            border: 1px solid var(--if-border-color);
            border-radius: 0.875rem;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 2px 4px -2px rgba(0, 0, 0, 0.05);
        }

        .contact-card {
            display: flex;
            align-items: center;
            padding: 1rem;
            border-bottom: 1px solid var(--if-border-color);
        }
        .contact-card:last-child { border-bottom: none; }

        .contact-avatar {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            background-color: var(--if-primary);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            font-size: 1.1rem;
            flex-shrink: 0;
        }

        .contact-info .icon-link {
            color: var(--if-text-muted);
            text-decoration: none;
            font-size: 0.9rem;
        }
        .contact-info .icon-link:hover { color: var(--if-primary); }
        
        .modal-content {
            border-radius: 0.875rem;
            border: 1px solid var(--if-border-color);
        }
        .modal-header {
            background-color: #f8f9fc;
            border-bottom-color: var(--if-border-color);
        }

        /* ===== ESTILO PARA EL LOGO DEL HEADER ===== */
        .header-logo {
            max-height: 45px;
            width: auto;
        }
        @media (max-width: 576px) {
            .header-logo {
                max-height: 35px;
            }
        }
    </style>
</head>
<body>

<div class="container py-5">
    <div class="row">
        <div class="col-lg-10 mx-auto">
            
            <div class="page-header mb-4 d-flex justify-content-between align-items-center flex-wrap">
                <div>
                    <h1 class="h3">Gestión de Contactos TI</h1>
                    <p class="text-muted mb-0">Administra la lista de contactos técnicos autorizados.</p>
                </div>
                <div class="mt-2 mt-sm-0">
                    <img src="/img/logo_paginas_cliente/logo386blanco.png" alt="Logo 386 SMART" class="header-logo">
                </div>
            </div>

            <div class="d-flex justify-content-end mb-3">
                <button class="btn btn-primary" id="btn-add-contact">
                    <i class="bi bi-plus-lg me-1"></i> Añadir Contacto
                </button>
            </div>
            <div class="mb-4">
                <div class="d-flex justify-content-between small mb-1">
                    <span class="fw-semibold">Límite de Registros</span>
                    <span id="contact-counter" class="text-muted">0 de <%= limit %></span>
                </div>
                <div class="progress" style="height: 8px;">
                    <div id="contact-progress-bar" class="progress-bar" role="progressbar" style="width: 0%;" aria-valuenow="0" aria-valuemin="0" aria-valuemax="<%= limit %>"></div>
                </div>
            </div>

            <div class="content-card">
                <div id="contacts-list-container">
                    </div>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="contact-modal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <form id="contact-form">
                <div class="modal-header">
                    <h5 class="modal-title fw-bold" id="modal-title"></h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <input type="hidden" id="contact-id">
                    <div class="mb-3">
                        <label for="contact-nombre" class="form-label fw-semibold">Nombre Completo</label>
                        <input type="text" class="form-control" id="contact-nombre" required>
                    </div>
                    <div class="mb-3">
                        <label for="contact-cel" class="form-label fw-semibold">Celular</label>
                        <input type="tel" class="form-control" id="contact-cel">
                    </div>
                    <div class="mb-3">
                        <label for="contact-correo" class="form-label fw-semibold">Correo Electrónico</label>
                        <input type="email" class="form-control" id="contact-correo">
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="submit" class="btn btn-primary" id="btn-save-contact"></button>
                </div>
            </form>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="/js/clients/contactos-ti.js"></script>
</body>
</html>