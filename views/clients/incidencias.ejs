<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Resumen de Incidencias</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="shortcut icon" href="/img/favicon-16x16.ico" type="image/x-icon">
    <style>
        :root {
            --if-primary: #0d6efd;
            --if-secondary: #6c757d;
            --if-text-main: #212529;
            --if-text-muted: #6c757d;
            --if-bg-main: #f8f9fc;
            --if-bg-panel: #ffffff;
            --if-border-color: #e2e8f0;
            --if-font-sans: 'Inter', sans-serif;
        }
        body {
            font-family: var(--if-font-sans);
            background-color: var(--if-bg-main);
            color: var(--if-text-main);
        }
        .main-content {
            padding: 2rem;
        }
        .page-header h1 {
            font-weight: 700;
        }
        .kpi-card {
            background-color: var(--if-bg-panel);
            border: 1px solid var(--if-border-color);
            border-radius: 0.875rem;
            padding: 1.5rem;
            display: flex;
            align-items: center;
            gap: 1rem;
        }
        .kpi-icon {
            font-size: 1.75rem;
            color: var(--if-primary);
            background-color: #e7f0ff;
            height: 50px;
            width: 50px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
        }
        .kpi-value {
            font-size: 2rem;
            font-weight: 700;
        }
        .kpi-label {
            font-size: 0.9rem;
            color: var(--if-text-muted);
        }
        .content-card {
            background-color: var(--if-bg-panel);
            border: 1px solid var(--if-border-color);
            border-radius: 0.875rem;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 2px 4px -2px rgba(0, 0, 0, 0.05);
        }
        .card-header-custom {
            padding: 1rem 1.5rem;
            border-bottom: 1px solid var(--if-border-color);
            display: flex;
            align-items: center;
            gap: 0.75rem;
            font-weight: 600;
        }
        .card-header-custom .bi {
            color: var(--if-primary);
        }
        .table thead {
            background-color: #f1f5f9;
        }

        /* ===== ESTILO PARA EL LOGO DEL HEADER ===== */
        .header-logo {
            max-height: 45px;
            width: auto;
        }
        @media (max-width: 576px) {
            .header-logo {
                max-height: 35px;
            }
        }
    </style>
</head>
<body>

<div class="main-content">

    <div class="page-header mb-4 d-flex justify-content-between align-items-center flex-wrap">
        <div>
            <h1 class="h3">Resumen de Incidencias y Servicios</h1>
            <p class="text-muted mb-0">Un vistazo general a la actividad de soporte técnico y piezas.</p>
        </div>
        <div class="mt-2 mt-sm-0">
            <img src="/img/logo_paginas_cliente/logo386blanco.png" alt="Logo 386 SMART" class="header-logo">
        </div>
    </div>
    <div class="row g-4 mb-4" id="kpi-container">
        <div class="col-md-4"><div class="kpi-card"><div class="spinner-border spinner-border-sm"></div></div></div>
        <div class="col-md-4"><div class="kpi-card"><div class="spinner-border spinner-border-sm"></div></div></div>
        <div class="col-md-4"><div class="kpi-card"><div class="spinner-border spinner-border-sm"></div></div></div>
    </div>

    <div class="row g-4">
        <div class="col-lg-5">
            <div class="content-card h-100">
                <div class="card-header-custom">
                    <i class="bi bi-tools"></i>
                    <span>Top Piezas Solicitadas</span>
                </div>
                <div class="card-body p-3">
                    <div id="piezasChartContainer" style="min-height: 400px;"></div>
                </div>
            </div>
        </div>
        <div class="col-lg-7">
            <div class="content-card h-100">
                <div class="card-header-custom">
                    <i class="bi bi-file-earmark-text"></i>
                    <span>Historial de Informes de Servicio</span>
                </div>
                <div class="card-body p-3">
                    <div class="row g-2 mb-3">
                        <div class="col-md-3">
                            <input type="text" id="filtroSerie" class="form-control form-control-sm" placeholder="N° Serie...">
                        </div>
                        <div class="col-md-3">
                            <input type="text" id="filtroTicket" class="form-control form-control-sm" placeholder="Ticket...">
                        </div>
                        <div class="col-md-3">
                            <input type="date" id="filtroFecha" class="form-control form-control-sm">
                        </div>
                        <div class="col-md-3">
                            <button id="btnBuscar" class="btn btn-primary btn-sm w-100"><i class="bi bi-search"></i> Buscar</button>
                        </div>
                    </div>
                    <div class="table-responsive">
                        <table class="table table-hover align-middle mb-0">
                            <thead>
                                <tr>
                                    <th class="py-3 px-3">Fecha</th>
                                    <th>Ticket</th>
                                    <th>N/S Equipo</th>
                                    <th>Técnico</th>
                                    <th class="text-end px-3">Acciones</th>
                                </tr>
                            </thead>
                            <tbody id="tabla-informes">
                                <tr><td colspan="5" class="text-center p-5"><div class="spinner-border"></div></td></tr>
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="card-footer bg-transparent py-3 px-3">
                    <nav id="pagination-container" class="d-flex justify-content-end">
                        <ul class="pagination pagination-sm mb-0"></ul>
                    </nav>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/apexcharts"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="/js/clients/incidencias.js"></script>

</body>
</html>