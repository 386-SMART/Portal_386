<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi Perfil</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="shortcut icon" href="/img/favicon-16x16.ico" type="image/x-icon">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <style>
        :root {
            --if-primary: #0d6efd;
            --if-secondary: #6c757d;
            --if-text-main: #212529;
            --if-text-muted: #6c757d;
            --if-bg-main: #f8f9fc;
            --if-bg-panel: #ffffff;
            --if-border-color: #e2e8f0;
            --if-font-sans: 'Inter', sans-serif;
        }
        body {
            font-family: var(--if-font-sans);
            background-color: var(--if-bg-main);
            color: var(--if-text-main);
        }
        .content-card {
            background-color: var(--if-bg-panel);
            border: 1px solid var(--if-border-color);
            border-radius: 0.875rem;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 2px 4px -2px rgba(0, 0, 0, 0.05);
        }
        .profile-header-card {
            display: flex;
            align-items: center;
            padding: 1.5rem;
            gap: 1.5rem;
        }
        .profile-logo {
            width: 80px;
            height: 80px;
            object-fit: contain;
            border: 1px solid var(--if-border-color);
        }
        .nav-tabs .nav-link {
            font-weight: 600;
            color: var(--if-text-muted);
        }
        .nav-tabs .nav-link.active {
            color: var(--if-primary);
        }
        .header-logo {
            max-height: 45px;
            width: auto;
        }
        @media (max-width: 576px) {
            .header-logo {
                max-height: 35px;
            }
        }
    </style>
</head>
<body>

<div class="container py-5">
    <div class="row">
        <div class="col-lg-10 mx-auto">
            
            <div class="page-header mb-4 d-flex justify-content-between align-items-center flex-wrap">
                <div>
                    <h1 class="h3 fw-bold">Configuración del Perfil</h1>
                    <p class="text-muted mb-0">Administra tus datos personales y contraseña.</p>
                </div>
                <div class="mt-2 mt-sm-0">
                    <img src="/img/logo_paginas_cliente/logo386blanco.png" alt="Logo 386 SMART" class="header-logo">
                </div>
            </div>

            <div class="content-card profile-header-card mb-4">
                <img src="<%= activeTenant.logo ? '/img/logo_cliente/' + activeTenant.logo : 'https://via.placeholder.com/150' %>" alt="logo" class="rounded profile-logo" id="profile-logo">
                <div>
                    <h4 class="fw-bold mb-0" id="display-name"><%= user.nombre %></h4>
                    <p class="text-muted mb-0"><%= activeTenant.empresaName %></p>
                </div>
            </div>

            <div class="content-card">
                <div class="card-header bg-transparent p-0">
                    <ul class="nav nav-tabs px-4 pt-3" id="profileTab" role="tablist">
                        <li class="nav-item" role="presentation">
                            <button class="nav-link active" data-bs-toggle="tab" data-bs-target="#datos-personales" type="button"><i class="bi bi-person-lines-fill me-2"></i>Datos Personales</button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#seguridad" type="button"><i class="bi bi-shield-lock-fill me-2"></i>Seguridad</button>
                        </li>
                        </ul>
                </div>
                <div class="card-body p-4">
                    <div class="tab-content" id="profileTabContent">
                        
                        <div class="tab-pane fade show active" id="datos-personales" role="tabpanel">
                            <form id="form-update-details">
                                <div class="mb-3">
                                    <label for="nombre-input" class="form-label fw-semibold">Nombre</label>
                                    <input type="text" class="form-control" id="nombre-input" value="<%= user.nombre %>" required>
                                </div>
                                <div class="mb-3">
                                    <label for="username-input" class="form-label fw-semibold">Username</label>
                                    <input type="text" class="form-control" id="username-input" value="<%= user.username %>" required>
                                </div>
                                <button type="submit" class="btn btn-primary float-end">Guardar Cambios</button>
                            </form>
                        </div>

                        <div class="tab-pane fade" id="seguridad" role="tabpanel">
                            <form id="form-update-password">
                                <div class="mb-3">
                                    <label for="current-password" class="form-label fw-semibold">Contraseña Actual</label>
                                    <input type="password" class="form-control" id="current-password" required>
                                </div>
                                <div class="mb-3">
                                    <label for="new-password" class="form-label fw-semibold">Nueva Contraseña</label>
                                    <input type="password" class="form-control" id="new-password" required>
                                </div>
                                <div class="mb-3">
                                    <label for="confirm-password" class="form-label fw-semibold">Confirmar Nueva Contraseña</label>
                                    <input type="password" class="form-control" id="confirm-password" required>
                                </div>
                                <button type="submit" class="btn btn-primary float-end">Cambiar Contraseña</button>
                            </form>
                        </div>

                        </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="/js/clients/perfil.js"></script>
</body>
</html>